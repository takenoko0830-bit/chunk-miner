# Chunk Miner — 引き継ぎ書

**作成日:** 2026-08-11
**最終更新:** 2026-08-11（Claude Code で編集機能・抽出上限・練習範囲フィルタ・GitHub Pages 対応を実装）
**目的:** このプロジェクトを Claude Code で継続するための設計背景・現状・次の一手の記録

---

## 1. 何を作っているか

英語の文章を入力すると再利用可能な「チャンク」に分解して蓄積し、そのチャンクから生成した例文を音声で流してリピート練習するアプリ。

利用者は日本の救急・集中治療医。目的は**医学英語のアウトプット力**、特に国際学会やカンファレンスの**質疑応答で口から出る英語**を増やすこと。読解や知識定着は目的ではない。

現状の実装は単一ファイル `index.html`（Vanilla JS、ビルド不要）。GitHub Pages で配信し、iPhone では
「ホーム画面に追加」でアプリとして使う。配信まわりの手順は `README.md` に分けた。

---

## 2. 設計の中核（ここは動かさないでほしい部分）

### 2.1 チャンクは3種類に分類する

| kind | 意味 | 例 | 展開 |
|---|---|---|---|
| `frame` | スロットを持つ型 | `There were no significant abnormalities in [X].` | よく効く |
| `colloc` | 語のまとまり | `perform an ABCD assessment` | ある程度効く |
| `fixed` | 文ごと覚える定型文 | `The airway was patent.` | ほぼ効かない |

**理由:** 粒度を区別せずに保存すると、ライブラリが重複と粒度バラバラで数ヶ月で使い物にならなくなる。`frame` を最上位に置くことで `we plan to repeat CT today` と `we plan to extubate tomorrow` が同一の型に正規化され、実例がぶら下がる構造になる。

### 2.2 スロットは `[X]` `[Y]` `[V]` で明示する

抽出時点でスロットを持たせないと、そこから例文が生成できない。`There were no significant abnormalities in the airway` をそのまま保存すると展開不能になる。**これが例文生成の質を決める最重要点。**

### 2.3 保存時に重複を統合し、`seen`（遭遇回数）を増やす

正規化キー `norm()` で照合（小文字化・スロットを `~` に・記号除去）。同じ型は新規追加せず `seen++`。

**理由:** 症例プレゼンを10本入れれば同じ型が8回出てくる。毎回別レコードにすると崩壊する。統合すれば逆に「よく出る型」が自動で浮かび上がり、`seen` 降順が**まず固めるべき30個**のリストになる。この指標がこのアプリの本質的な価値。

### 2.4 評価・採点はしない

ユーザーが明示的に不要と判断。音声認識も使わない。聞いて口に出すだけ。手が塞がっていても使えることを優先する。

### 2.5 個人情報を入力しない

入力は英文テキストのみで、患者情報を含まないことを利用者が目視確認する運用。API 送信前の自動フィルタは実装していない。**この前提を崩す変更（録音の直接取り込み等）は施設の情報セキュリティ規程の確認なしに進めないこと。**

---

## 3. 検討して却下した案（再提案しないこと）

| 案 | 却下理由 |
|---|---|
| カンファ録音 → ASR → 話者分離 → 自動教材化 | 患者情報を含むため要ローカル完結、会議室の実音質で Whisper 精度が担保できない、実装量が3倍。**将来の選択肢としては生きているが優先度は低い** |
| 日本語を入力して英訳し、そこからチャンクを取る | 日本語の型に最適化された発話を英訳しても、英語話者が同じ場面で使う構造にならない。質疑応答で最も効く発話ほど取りこぼす |
| 3秒以内に応答する反応速度トレーニング | 設計としては有効だが、ユーザーが評価機能不要と判断。v2 以降の候補 |
| 音声認識による回答チェック | 同上。加えて Web Speech API は医学用語（norepinephrine, prerenal 等）を誤認識する |
| React artifact 版（`chunk_miner.jsx`） | claude.ai 内でしか API が通らず、リロードで消える。HTML 版に置き換え済み。**参照不要** |

---

## 4. 現状の実装

### 4.1 ファイル

```
index.html            # これ1枚で完結。外部依存は Google Fonts のみ
chunks.json           # 共有ライブラリの正本。各端末が起動時に取得して統合する
manifest.webmanifest  # ホーム画面に追加したときのアプリ定義
sw.js                 # ネットワーク優先のサービスワーカー。オフラインで練習タブが動く
icons/                # 180 / 192 / 512 の PNG。スロット記法 [■] のモチーフ
README.md             # ローカル起動と GitHub Pages へのデプロイ手順
chunks-YYYY-MM-DD.json # 各回の抽出結果。chunks.json に統合済みの作業ファイル
```

サービスワーカーはネットワーク優先にしてある。デプロイした新版は次に開いたときそのまま反映され、
キャッシュを手で消す必要はない。`sw.js` の `V` を上げるのは古いキャッシュを捨てたいときだけでよい。

### 4.2 技術

- Vanilla JS、ビルドなし。`render()` で全画面を再描画する単純な構成
- 音声: Web Speech API (`speechSynthesis`)。声は端末依存（Mac は Samantha / Ava 推奨）
- 保存: `localStorage`（`store` ラッパで try/catch。使えない環境ではメモリのみで動作し警告を表示）
- API: Anthropic Messages API を**ブラウザから直接**呼ぶ

```js
headers: {
  'content-type': 'application/json',
  'x-api-key': apiKey,
  'anthropic-version': '2023-06-01',
  'anthropic-dangerous-direct-browser-access': 'true'   // これがないと CORS で落ちる
}
```

モデル: `claude-sonnet-5`（既定）/ `claude-haiku-4-5-20251001` / `claude-opus-5` から選択。

### 4.3 データモデル

```js
chunk = {
  id: string,
  text: string,          // "There were no significant abnormalities in [X]."
  kind: 'frame' | 'colloc' | 'fixed',
  ja: string,            // 日本語の意味
  examples: [{ en, ja }],
  on: boolean,           // 練習キューに含めるか
  seen: number,          // 遭遇回数
  added: number          // timestamp（ソート用）
}
```

localStorage キー: `cm.chunks` / `cm.set` / `cm.key` / `cm.model` / `cm.raw` / `cm.plays`

`cm.plays` は例文ごとの練習回数。**キーは英文そのもの**で、チャンク id や配列の添字ではない。
同期・並べ替え・再取り込みで壊れないようにするため。**読み上げ1回ごとに加算**するので、
繰り返しを3回にしていれば1周で3増える（総再生回数）。途中で停止した分は数えない。

`cm.set`（settings）には再生設定に加えて `topN`（練習範囲の絞り込み。0 は絞らない）と
`maxCand`（1回の抽出で出す上限）が入る。

### 4.4 画面

- **取り込み** — 英文貼り付け → LLM が抽出 → 候補をチェックで取捨 → 保存（既存型は `seen++`）。
  抽出の上限は 3 / 5 / 8 / 12 から選ぶ（既定 5）。モデルが上限を超えて返しても `slice` で切り詰める
- **チャンク** — 一覧、オンオフ、**編集（text / ja / kind）**、例文追加生成、削除、ソート（追加順 / 遭遇回数順 / 例文数順）
- **練習** — 例文を連続再生。**レッスン（10 / 20 / 30文 / 区切らない。既定20）**、
  **考える間（なし / 3秒 / 5秒 / 自動。既定3秒）**、練習範囲（全部 / 上位10 / 20 / 30）、
  繰り返し 1〜5回、復唱の間（なし / 短め1.2 / 標準1.8 / 長め2.5。英文長×係数で自動計算）、
  日本語の表示/読み上げ/なし、順序（順番 / シャッフル / 少ない順）、ループ。
  例文ごとの総再生回数を表示する。**速度の切り替えは廃止**（常に標準）。英語の声は Samantha を優先

### 4.5 練習の設計（考える間とレッスン）

**考える間**は日本語の手がかりのあとに置き、その間 `.en` に `veil` を当てて英文を伏せる
（`visibility:hidden` なので高さは保たれ画面が跳ねない）。読めてしまうと引き出す練習にならないため。
聞いて真似る（シャドーイング）から、自分で言ってから答え合わせをする（プロダクション）練習に変わる。
**目的が「質疑応答で口から出る英語」である以上、聞いた回数より自分で引き出した回数が効く。**
`jaMode='off'` のときは手がかりがないので働かせない。

**レッスン**は決めた文数で `run()` を打ち切る。終了時に `idx` を次の位置へ進めるので、
「次のレッスン」を押すと続きから始まり、同じ範囲を繰り返さない。1日に複数レッスンを想定している。
無限ループは規模が増えると一度も回りきらないため、終わりを作ることに意味がある
（67例文・繰り返し2回・復唱の間「標準」で1周およそ25分。300例文なら2時間）。
- **設定** — APIキー、モデル、JSON 書き出し／読み込み（統合方式）、**共有ライブラリの手動取り込み**、初期化

### 4.6 共有ライブラリ（端末間の同期）

`chunks.json` が正本で、アプリは起動時にこれを取得して統合する（`syncLibrary()`）。
**リポジトリ → 端末の一方向。** Mac で採ったチャンクを push すれば、iPhone はアプリを開くだけで反映される。
端末側の編集・オンオフ・削除は正本には戻らない。

運用は「英文を Claude Code に渡す → `chunks.json` に統合 → `git push`」。
`chunks-YYYY-MM-DD.json` は各回の抽出結果を残したもので、正本ではない。

### 4.7 実装上の注意

- **編集の衝突チェック**: `text` を変更したとき、`norm()` が他のチャンクと一致したら保存を拒否する。
  ここを緩めると 2.3 の重複統合が壊れる
- **練習範囲は `activeChunks()` に集約**してある。`on` で絞ってから `seen` 降順で上位 N 件を取る。
  再生キューを作る `build()` と、画面に出す件数表示の両方がこれを使う
- **iOS の音声解禁**: `prime()` がクリックハンドラの同期実行中に音量0の発話を1回流している。
  これがないと iOS では以降すべて無音になる。`toggle()` / `jump()` の先頭から呼ぶこと
- **`SEED` は必ず複製して使うこと**: `store.get('cm.chunks', SEED)` のように既定値をそのまま渡すと、
  localStorage が空の初回起動で `chunks` と `SEED` が同じ配列を指す。以降の `unshift` や編集が
  定数 `SEED` を書き換えてしまい、「初期状態に戻す」が汚染された SEED を復元することになる
- **起動は常に練習タブ**: 以前は `navigator.standalone` で判定していたが、効かない環境があったため無条件にした
- **`loadVoices()` から初回 render() より前に描画しないこと**: `loadVoices()` はスクリプト評価中にも
  走る。その時点では `const TABS` などがまだ TDZ にあり、`render()` を呼ぶと
  `ReferenceError: Cannot access 'TABS' before initialization` でスクリプト全体が停止し、
  ヘッダだけ出て中身が空の画面になる。`booted` フラグで抑止している。
  **Safari は `getVoices()` が同期で返るため必ず踏む。Chromium 系は読み込み時点で空を返すので
  再現しない** — ブラウザ差で通り抜けるので、この種の初期化順の変更は Safari で確認すること
- **同期の `seen` は加算しないこと**: `importJSON()` は `hit.seen += (p.seen||1)` と加算するが、
  `syncLibrary()` は `Math.max(ローカル, リモート)` を取る。同期は起動のたびに走るので、
  加算にすると**アプリを開くだけで遭遇回数が際限なく増え、2.3 の指標が壊れる**。
  ここを「読み込みと同じ処理でいいだろう」と統一してはいけない

### 4.8 初期データ

トラウマ症例プレゼンの英文を解析した 14 チャンクを同梱。APIキーなしでも練習タブは動く。

---

## 5. 既知の問題・未解決事項

**解決済み（2026-08-11）:**

- ~~iPhone で使えない~~ → GitHub Pages 配信 + PWA 化。手順は README.md
- ~~抽出が多すぎる~~ → 上限セレクタ（既定5）と選別基準の強化。「上限であって目標ではない」を明示
- ~~保存後に編集できない~~ → チャンクタブに編集フォーム（text / ja / kind）
- ~~`seen` を活かした画面がない~~ → 練習タブの「練習範囲」で上位 N 件に絞り込み

- ~~端末間で同期しない~~ → `chunks.json` を正本とする一方向同期。起動時に自動取得（4.5節）

**残っている問題、優先度順:**

1. **同期が一方向。** 端末側での編集・オンオフ・削除は正本に戻らない。双方向にするなら Gist か Supabase が要る。
   また `chunks.json` の更新には Mac とターミナルが要る（iPhone だけでは完結しない）
2. **iOS は画面ロックで読み上げが止まる。** `speechSynthesis` の制約で回避できない。

   **検証済みの代替案:** 例文を `say -v Samantha -o x.m4a --data-format=aac` で音声ファイル化し、
   `<audio>` で再生すればロック中も鳴る（iOS が止めるのは読み上げであってメディア再生ではない）。
   MediaSession API でロック画面に操作も出せる。生成は1文あたり1秒未満・約50KB、46例文で約2.3MB。
   ファイル名を英文のハッシュにすれば git 履歴が膨らまない。ネイティブアプリ化は不要。

   **2026-08-11 時点では見送り。** 音声を焼き込むと声が固定され、端末ごとに声を選べる利点が
   失われるため。利用者が「声を変えられることは利点」と判断した。バックグラウンド再生の必要性が
   声の自由より上回った時点で再検討する。実装方針は上記のとおり確定している
3. **例文を個別に消せない。** 「例文を増やす」は4件まとめて生成するので、外れが混ざると練習キューに残り続ける。チャンク本体は編集できるようになったが、例文はまだ全消し（チャンクごと削除）しかない
4. **抽出上限の実効性は未検証。** プロンプトを厳しくしたが、実際に3〜5個に落ち着くか、落ちた分が本当に不要なものかは英文を通して確かめていない
5. **APIキーが localStorage 平文。** 個人端末専用の前提。共有端末では使わない旨を設定タブに明記済み

---

## 6. 次にやるべきこと

**まず検証（実装より先）**

英文を 10〜20 本通して、以下を見る。ここが確認できるまで機能追加しない方がよい。

- 抽出の粒度が実用に足るか（スロットの切り方が妥当か）
- `seen` が実際に伸びるか、伸びた型が納得できるものか
- 生成される例文が「読んで納得できる」だけでなく、数日後に口から出るか

検証で「抽出上限5でちょうどよいか」も併せて見る。多すぎるなら3に、足りないなら8に振る。

**実装候補（優先度順）**

1. 例文の個別削除（生成4件のうち外れを捨てられるようにする）
2. 例文の音声ファイル書き出し（iOS バックグラウンド再生の回避策）
3. 双方向同期（Gist / Supabase。一方向で足りているうちは不要）

**済んだもの:** 静的ホスティング対応、チャンクの編集、抽出上限と選別基準、練習範囲フィルタ、一方向同期

**将来的な拡張（優先度低）**

- 質疑応答の頻出「発話意図」タグによる分類（保留・反論・確認・根拠を尋ねる 等）。以前の設計案では意図タグを主キーにする構想があった。40〜60個で質疑の8割が回るという見立て
- 3秒レスポンス訓練モード
- カンファ録音からの自動取り込み（要ローカル ASR、要施設ルール確認）

---

## 7. Claude Code での作業開始時の指示例

```
index.html を読んで、引き継ぎ書 HANDOVER.md の設計方針に沿って作業する。
特に以下は変更しないこと:
- チャンクの3分類（frame / colloc / fixed）
- スロットの [X] 表記
- 保存時の重複統合と seen カウンタ
- 評価・採点機能は追加しない
```

---

## 8. 補足：この設計に至った経緯（要約）

初期案はカンファレンスの録音を文字起こしして教材化するものだった。患者情報と音質の2点が壁になり、次に「日本語で言いたかったことを入力して英訳する」案に移った。しかし質疑応答を主目的に据えた段階で、日本語起点の翻訳では英語話者が実際に使う発話構造を取りこぼすことが判明。最終的に**英語を英語のまま入力してチャンクに分解する**現在の形に落ち着いた。

この経緯の要点は、**素材が最初から正しい英語であれば、翻訳品質という最大のボトルネックが消える**こと。入力の手間は増えるが、出力の質が安定する。
